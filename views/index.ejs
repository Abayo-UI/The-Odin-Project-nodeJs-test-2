<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Mini Message Board</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            display: flex;
            justify-content: center;
            margin: 0;
            padding: 40px 20px;
            font-family: Arial, sans-serif;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            width: 100%;
            max-width: 600px;
        }

        .message {
            border: 1px solid #000;
            padding: 10px;
            margin-bottom: 10px;
            width: 100%;
            box-sizing: border-box; /* ensures padding doesn't exceed width */
        }

        .message h4 {
            margin: 0 0 5px 0;
            font-size: 1rem;
        }

        .message span {
            color: gray;
            font-size: 0.9rem;
        }

        a.add-message {
            margin-bottom: 15px;
            text-decoration: none;
            color: blue;
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="/new" class="add-message">Add new message Here</a>

        <% messages.forEach(message => { %>
            <div class="message">
                <h4>
                    <%= message.user %>: 
                    <span><%= message.text %></span>
                </h4>
                <small>
                    <%= new Date(message.added).toLocaleString("en-US", {
                        day: "numeric", 
                        month: "long", 
                        year: "numeric", 
                        hour: "2-digit", 
                        minute: "2-digit", 
                        second: "2-digit"
                    }) %>
                </small>
            </div>
        <% }) %>
    </div>
</body>
</html>
